package removeassign.good;

import removeassign.bad.Order;

public class Main {
    double calculateDiscount(Order order, double totalPrice){
        // no modificar los datos de entradas como totalPrice
        double result = totalPrice;
        if(Order == null || order.getPrice() = null){
            return result;
        }

        if(order.getPrice() > 100){
            totalPrice += order.getPrice() * order.getOffer();
            return result;
        }
    }
}
